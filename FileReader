import java.io.BufferedReader;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.util.ArrayList;


public class FileReader 
{
    //The filename that you are reading in.  
    //The file must be located in the same directory as your code
    public static final String FILENAME = "Example1.txt";
    
    
    // public static void main(String[] args) 
    // {
        // //The "lines" ArrayList will read in each line from the file
        // //and store each line as a String. 
        // ArrayList<String> lines = new ArrayList<String>();    

        
        // //READS IN THE FILE INPUT **************************
        // try{
            // //read the file in as a List of String objects
            // lines = readFile(FILENAME);
        // }
        // catch(Exception e){
            // e.printStackTrace();
        // }
        // //***************************************************
     
        // /** Your code will go here. **/
        
        // //Printing out the contents of the ArrayList
        // //Feel free to delete this.
        // for (String s : lines)
            // System.out.println(s);
        
        
            
            
            
            
            
    // }//END MAIN
    

    //Pre-written method used in the main to read in the file, line by line.
    //The only change that needs to be made is the class name in the "try" block
    private static ArrayList<String> readFile(String fileName) throws Exception{
        ArrayList<String> retVal = new ArrayList<String>();
        
        try{
            //read file
            InputStream in = FileReader.class.getResourceAsStream(fileName);
            BufferedReader br = new BufferedReader(new InputStreamReader(in));
    
            //loop through lines of input file
            String s;
            while((s = br.readLine()) != null) { 
                retVal.add(s);
            }
        }catch(Exception e){
            throw e;
        }
        
        return retVal;
    }

}
